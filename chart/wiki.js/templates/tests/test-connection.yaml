apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wiki.js.fullname" . }}-test-connection"
  labels:
    {{- include "wiki.js.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wiki.js.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
